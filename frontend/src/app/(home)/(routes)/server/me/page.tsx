const ServerPage = async () => {
  return (
    <div
      className={
        "w-full h-full bg-card/50 text-2xl flex items-center justify-center"
      }
    >
      Not built yet.
    </div>
  );
};

export default ServerPage;
